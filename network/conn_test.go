package network

import (
	. "github.com/onsi/ginkgo"
	// . "github.com/onsi/gomega"
)

var _ = Describe("Conn", func() {
	Describe("readPayload", func() {
		It("should do something", func() {
			Skip("Write test(s) for this...")
		})
	})

	Describe("Close", func() {
		It("should do something", func() {
			Skip("Write test(s) for this...")
		})
	})

	Describe("ReadHello", func() {
		It("should do something", func() {
			Skip("Write test(s) for this...")
		})
	})

	Describe("ReadMessage", func() {
		It("should do something", func() {
			Skip("Write test(s) for this...")
		})
	})

	Describe("WritePayload", func() {
		It("should do something", func() {
			Skip("Write test(s) for this...")
		})
	})

	Describe("WriteRequest", func() {
		It("should do something", func() {
			Skip("Write test(s) for this...")
		})
	})

	Describe("AbnormalError", func() {
		It("should do something", func() {
			Skip("Write test(s) for this...")
		})
	})

	Describe("NewConn", func() {
		It("should do something", func() {
			Skip("Write test(s) for this...")
		})
	})
})
