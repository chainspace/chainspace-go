package config

import (
	. "github.com/onsi/ginkgo"
	// . "github.com/onsi/gomega"
)

var _ = Describe("Config", func() {
	Describe("Hash", func() {
		It("should do something", func() {
			Skip("Write test(s) for this...")
		})
	})

	Describe("URL", func() {
		It("should do something", func() {
			Skip("Write test(s) for this...")
		})
	})

	Describe("LoadContracts", func() {
		It("should do something", func() {
			Skip("Write test(s) for this...")
		})
	})

	Describe("LoadKeys", func() {
		It("should do something", func() {
			Skip("Write test(s) for this...")
		})
	})

	Describe("LoadNetwork", func() {
		It("should do something", func() {
			Skip("Write test(s) for this...")
		})
	})

	Describe("LoadNode", func() {
		It("should do something", func() {
			Skip("Write test(s) for this...")
		})
	})
})
