package sbac

import (
	. "github.com/onsi/ginkgo"
	// . "github.com/onsi/gomega"
)

var _ = Describe("PendingEvents", func() {
	Describe("Close", func() {
		It("should do something", func() {
			Skip("Write test(s) for this...")
		})
	})

	Describe("Len", func() {
		It("should do something", func() {
			Skip("Write test(s) for this...")
		})
	})

	Describe("OnEvent", func() {
		It("should do something", func() {
			Skip("Write test(s) for this...")
		})
	})

	Describe("Run", func() {
		It("should do something", func() {
			Skip("Write test(s) for this...")
		})
	})

	Describe("NewPendingEvents", func() {
		It("should do something", func() {
			Skip("Write test(s) for this...")
		})
	})
})
