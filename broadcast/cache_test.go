package broadcast

import (
	. "github.com/onsi/ginkgo"
	// . "github.com/onsi/gomega"
)

var _ = Describe("Cache", func() {
	Describe("get", func() {
		Context("when ownBlocks", func() {
			It("should do something", func() {
				Skip("Write test(s) for this...")
			})
		})

		Context("when receivedMap", func() {
			It("should do something", func() {
				Skip("Write test(s) for this...")
			})
		})
	})

	Describe("prune", func() {
		It("should do something", func() {
			Skip("Write test(s) for this...")
		})
	})

	Describe("set", func() {
		Context("when ownBlocks", func() {
			It("should do something", func() {
				Skip("Write test(s) for this...")
			})
		})

		Context("when receivedMap", func() {
			It("should do something", func() {
				Skip("Write test(s) for this...")
			})
		})
	})

	Describe("setSequence", func() {
		It("should do something", func() {
			Skip("Write test(s) for this...")
		})
	})
})
